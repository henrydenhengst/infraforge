---
# =============================================================================
# BUDIBASE-AI - INTERNE TOOLS MET AI
# =============================================================================
# Beschrijving: Interne tools genereren met AI-ondersteuning
# Website: https://budibase.com
# =============================================================================

application:
  name: budibase-ai
  version: 2.12.0
  description: "Budibase-AI - Interne tools met AI"
  environment: "{{ environment | default('production') }}"
  
  owner:
    team: development-team
    contact: dev@company.local
    slack: "#development"
    documentation: "https://docs.budibase.com"
    
  hardware_requirements:
    compute:
      min_cpu_cores: 2
      min_ram_gb: 2
      recommended_cpu_cores: 4
      recommended_ram_gb: 4
      
    storage:
      - name: data
        size_gb: 10
        performance: ssd
        backup_required: true
        mount_path: /data
        
  network_requirements:
    vlans:
      - development
      - internal
    ports:
      - container: 80
        host: 8081
        protocol: tcp
        public: false
        description: "Budibase-AI web interface"
    dns:
      - "budibase-ai.{{ domain }}"
      
  container:
    image: budibase/budibase:latest
    restart: unless-stopped
    resources:
      limits:
        cpu: "4"
        memory: 4Gi
      requests:
        cpu: "2"
        memory: 2Gi
        
  environment_variables:
    - name: BUDIBASE_AI_ENABLED
      value: "true"
      secret: false
    - name: AI_PROVIDER
      value: "openai"
      secret: false
    - name: AI_API_KEY
      value: "{{ vault_openai_key }}"
      secret: true
    - name: BUDIBASE_COUCHDB_PASSWORD
      value: "{{ vault_budibase_couch_password }}"
      secret: true
    - name: BUDIBASE_MINIO_SECRET_KEY
      value: "{{ vault_minio_password }}"
      secret: true
    - name: BUDIBASE_REDIS_PASSWORD
      value: "{{ vault_redis_password }}"
      secret: true
      
  health_checks:
    liveness:
      type: http
      path: /health
      port: 80