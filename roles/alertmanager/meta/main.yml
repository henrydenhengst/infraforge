---
galaxy_info:
  author: "{{ company_name }}"
  description: "Prometheus Alertmanager for alert routing"
  license: "Proprietary"
  min_ansible_version: 2.9
  galaxy_tags: [alertmanager, alerting, monitoring, prometheus]
dependencies:
  - docker
