---
- name: Configureer VLAN interfaces
  ansible.builtin.template:
    src: vlan_alpine.j2
    dest: /etc/network/interfaces
    mode: '0644'
  loop: "{{ network_vlans }}"
  notify: restart networking
