---
- name: Configureer VLAN interfaces
  ansible.builtin.template:
    src: vlan_redhat.j2
    dest: "/etc/sysconfig/network-scripts/ifcfg-{{ item.parent }}.{{ item.vlan_id }}"
    mode: '0644'
  loop: "{{ network_vlans }}"
  notify: restart networking
