---
galaxy_info:
  author: "{{ company_name }}"
  description: "Logstash log processing pipeline"
  license: "Proprietary"
  min_ansible_version: 2.9
  galaxy_tags: [logstash, logging, pipeline, elk]
dependencies:
  - docker
