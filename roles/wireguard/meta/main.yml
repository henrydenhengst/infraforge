---
galaxy_info:
  author: "{{ company_name }}"
  description: "WireGuard VPN server"
  license: "Proprietary"
  min_ansible_version: 2.9
  galaxy_tags: [wireguard, vpn, network]
dependencies:
  - docker
