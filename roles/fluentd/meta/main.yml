---
galaxy_info:
  author: "{{ company_name }}"
  description: "Fluentd unified logging layer"
  license: "Proprietary"
  min_ansible_version: 2.9
  galaxy_tags: [fluentd, logging, logs, collector]
dependencies:
  - docker
